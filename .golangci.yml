version: "2"

linters:
  enable:
    - lll

  settings:
    lll:
      line-length: 120
      tab-width: 4

  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - examples
